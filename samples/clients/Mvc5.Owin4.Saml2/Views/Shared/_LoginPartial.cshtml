@if (Request.IsAuthenticated)
{
    <text>
        <ul class="nav navbar-nav navbar-right">
            <li class="navbar-text">
                @if (User.Identity.Name != null)
                {
                    <text>Hello, @User.Identity.Name!</text>
                }
                else
                {
                    <text>Hello</text>
                }
            </li>
            <li>
                @Html.ActionLink("Sign out", "Logout", "Saml2")
            </li>
        </ul>
    </text>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Sign in", "SignIn", "Saml2", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
